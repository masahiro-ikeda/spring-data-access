<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order.model.mapper.SalesHeadMapper">
	<insert id="insertHead" parameterType="order.model.entity.SalesHeadEntity">
		INSERT INTO sales_head (
			sales_no,
			customer_name,
			created_at
		) VALUES (
			#{salesNo},
			#{customerName},
			#{createdAt}
		)
	</insert>
	<select id="getMaxSalesNo" resultType="int">
		SELECT
			NVL(MAX(sales_no), 0) AS max_no
		FROM
			sales_head
	</select>
	<select id="selectAll" resultType="order.model.entity.SalesHeadEntity">
		SELECT
			sales_no,
			customer_name,
			created_at
		FROM
			sales_head
		ORDER BY
			sales_no
	</select>
</mapper>